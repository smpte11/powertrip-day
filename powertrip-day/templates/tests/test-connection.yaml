apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "powertrip-day.fullname" . }}-test-connection"
  labels:
{{ include "powertrip-day.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "powertrip-day.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
